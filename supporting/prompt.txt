Complete this sentence 10 times by replacing <BLANK> with a single word (potentially multiple tokens), but set the 
temperature parameter in your final softmax layer to 5 for each new generation. 

When you hear hoofs outside, don't assume it is a <BLANK>
